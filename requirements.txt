# ============================================================
# 用户研究综合分析工具 · 依赖说明
# ============================================================
#
# 本项目为纯前端单文件 HTML 工具，无 Python 后端依赖。
# 直接用浏览器打开 user-research-skill.html 即可运行。
#
# ============================================================
# 前端外部库（运行时通过 CDN 动态加载，需网络连接）
# ============================================================
#
#   mammoth.js  v1.6.0   .docx 文件文本提取
#   CDN: https://cdnjs.cloudflare.com/ajax/libs/mammoth/1.6.0/mammoth.browser.min.js
#
#   SheetJS xlsx  v0.18.5   .xlsx / .xls 文件解析
#   CDN: https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js
#
# ============================================================
# 离线降级说明
# ============================================================
#
#   若无网络，.docx 和 .xlsx 解析将失败并显示提示。
#   .txt 和 .csv 文件完全离线可用，不依赖任何外部库。
#   建议在无网络环境下将访谈文件转存为 .txt 格式。
#
# ============================================================
# 测试脚本依赖（如需运行 Python 辅助脚本）
# ============================================================
#
# 目前无 Python 测试脚本，以下为预留占位：
# requests>=2.31.0       # 若需要 Python 侧调用 API 验证
# python-docx>=1.1.0     # 若需要 Python 侧解析 .docx 文件
# openpyxl>=3.1.2        # 若需要 Python 侧解析 .xlsx 文件
#
# 安装方式（如需要）：
#   pip install requests python-docx openpyxl
